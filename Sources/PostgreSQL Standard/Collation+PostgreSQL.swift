import Structured_Queries_Primitives

extension Collation {
    /// The built-in `C` collation.
    public static let C = Self(rawValue: "C")

    /// The built-in `POSIX` collation.
    public static let POSIX = Self(rawValue: "POSIX")
}
